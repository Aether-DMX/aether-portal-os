/**
 * Modal components index
 *
 * NOTE: PlaySceneModal and PlayChaseModal were removed - they were dead code.
 * Both Scenes and Chases now use the unified ApplyTargetModal component directly.
 */
export { default as ConfirmModal } from './ConfirmModal';
